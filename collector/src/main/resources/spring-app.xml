<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xmlns:context="http://www.springframework.org/schema/context"
       xmlns:task="http://www.springframework.org/schema/task" xmlns:jdbc="http://www.springframework.org/schema/jdbc"
       xmlns:tx="http://www.springframework.org/schema/tx"
       xsi:schemaLocation="http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans.xsd
        http://www.springframework.org/schema/context http://www.springframework.org/schema/context/spring-context.xsd http://www.springframework.org/schema/task http://www.springframework.org/schema/task/spring-task.xsd http://www.springframework.org/schema/jdbc http://www.springframework.org/schema/jdbc/spring-jdbc.xsd   http://www.springframework.org/schema/tx http://www.springframework.org/schema/tx/spring-tx.xsd">

    <context:property-placeholder location="classpath:app.properties,file:${conf.dir}/collector.cfg"
                                  system-properties-mode="NEVER"
                                  ignore-resource-not-found="true"/>

    <context:component-scan base-package="me.alexand.scat.statistic.collector"/>
    <tx:annotation-driven/>
    <task:annotation-driven scheduler="poolScheduler"/>
    <task:scheduler id="poolScheduler" pool-size="4"/>

    <bean id="hsqldbDataSource" class="org.apache.commons.dbcp2.BasicDataSource">
        <property name="driverClassName" value="${db.hsqldb.driverClassName}"/>
        <property name="url" value="${db.hsqldb.url}"/>
        <property name="username" value="${db.hsqldb.username}"/>
        <property name="password" value="${db.hsqldb.password}"/>
        <property name="initialSize" value="${processors.count}"/>
        <property name="maxTotal" value="${processors.count}"/>
    </bean>

    <bean id="postgresqlDataSource" class="org.apache.commons.dbcp2.BasicDataSource">
        <property name="driverClassName" value="${db.postgresql.driverClassName}"/>
        <property name="url" value="${db.postgresql.url}"/>
        <property name="username" value="${db.postgresql.username}"/>
        <property name="password" value="${db.postgresql.password}"/>
        <property name="initialSize" value="${processors.count}"/>
        <property name="maxTotal" value="${processors.count}"/>
    </bean>

    <bean id="bufferTM" class="org.springframework.jdbc.datasource.DataSourceTransactionManager">
        <property name="dataSource" ref="hsqldbDataSource"/>
    </bean>

    <bean id="postgresqlTM" class="org.springframework.jdbc.datasource.DataSourceTransactionManager">
        <property name="dataSource" ref="postgresqlDataSource"/>
    </bean>

    <jdbc:initialize-database data-source="hsqldbDataSource">
        <jdbc:script location="classpath:db/initBuffer.sql"/>
    </jdbc:initialize-database>

    <jdbc:initialize-database data-source="postgresqlDataSource">
        <jdbc:script location="classpath:db/initPostgres.sql"/>
    </jdbc:initialize-database>

    <bean id="bufferJDBCTemplate" class="org.springframework.jdbc.core.JdbcTemplate">
        <constructor-arg ref="hsqldbDataSource"/>
    </bean>

    <bean id="postgresqlJDBCTemplate" class="org.springframework.jdbc.core.JdbcTemplate">
        <constructor-arg ref="postgresqlDataSource"/>
    </bean>
</beans>